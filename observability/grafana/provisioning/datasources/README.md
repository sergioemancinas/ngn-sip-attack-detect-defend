# Grafana Datasources

Grafana datasources are provisioned from this directory.

- `clickhouse.yml`: ClickHouse datasource with stable UID `clickhouse`.
- `prometheus.yml`: Prometheus datasource with stable UID `prometheus`.

OpenSearch/Wazuh dashboard datasources are deferred.
